# CLAUDE.md — рабочие правила для Claude Code

Этот файл — точка входа для Claude Code в проект. Он не заменяет документы в `docs/`,
а только задаёт правила работы с ними и даёт карту, что где искать. Всё содержательное
(структура полей, конкретные значения, формулировки) — в самих документах `docs/`, не здесь.

## О проекте
B2B интернет-магазин промышленного и специализированного оборудования (газоанализаторы,
КИП, лабораторное оборудование, спецтехника), импортируемого из Китая. Склада в РФ нет —
всё под заказ. Клиенты — юридические лица, оплаты на сайте нет. Флоу — два равноправных
пути, ни один не собирает персональные данные клиента на сайте (см. ниже):
- (а) быстрый запрос по одному товару прямо с карточки — переход на
  `/contacts?product=<slug>`, дальше клиент связывается с менеджером напрямую
  (телефон/email, указанные на сайте);
- (б) корзина (`/cart`, решено 2026-09-06, см. `docs/architecture.md`, раздел 2.3) —
  клиент добавляет несколько интересующих товаров, состояние хранится только в
  `localStorage` браузера (без сервера/БД), и сам передаёт менеджеру готовую сводку —
  через `mailto:` (открывает его собственный почтовый клиент) или копированием текста.

В обоих случаях менеджер вручную готовит КП и согласовывает заказ у поставщика; сайт не
собирает персональные данные ни в одном из путей — email из корзины не проходит через
сервер сайта. Форма заявки и автоматизация «заявка → PDF КП → email менеджеру» (это была
серверная автоматизация, не сами пути (а)/(б) выше) убраны юридическим решением от
2026-08-31 (152-ФЗ — не становиться оператором персональных данных). Личный кабинет
и авторизация на этом этапе не реализуются.

## Стек и ключевые ограничения
- Next.js (App Router) + TypeScript + Tailwind CSS, деплой на Vercel (автодеплой из GitHub,
  `main` → Production, остальные ветки → Preview)
- Отдельного бэкенда нет — вся серверная логика внутри route handler'ов Next.js
- Каталог — локальные TS/JSON-данные в `lib/data/`, без CMS и без сетевого слоя
- «Живых» сетевых сценариев на сайте нет — это статичная каталог-витрина. Форма заявки
  (`POST /api/request`) и фоновая генерация КП (`after()` + Vercel Blob) отменены
  юридическим решением от 2026-08-31; `docs/api.md` и `docs/commercial-offer.md` помечены
  как устаревшие
- TypeScript strict mode, ESLint + Prettier — единый конфиг репозитория
- Node.js версия зафиксирована в `package.json` (`engines`)
- Алиас путей `@/*` → `src/*`

## Правила кода — чего НЕ делать
- **Не вводить абстракции "про запас"**: ни слой над источником каталожных данных (на случай
  будущей CMS), ни обобщённую "сетевую обёртку". Решаем текущую задачу, не гипотетическую
  будущую.
- **Не заводить дублирующие типы.** `Product`, `Category` объявлены один раз
  в `types/` и импортируются везде — новый интерфейс с тем же смыслом в другом месте не создавать.
  (`RequestPayload` / `types/request.ts` относятся к отменённой форме заявки — не использовать.)
- `components/ui/*` не обращается к `lib/data` и не содержит бизнес-логики — только пропсы
  и вёрстка. Работа с данными — в `components/features/*` или `page.tsx`.
- Не использовать `/request` — оплаты и сбора заявок на сайте нет. Основной призыв к
  действию (шапка, карточка товара) — простая ссылка на `/contacts`, без `tel:`/`mailto:`
  и без форм сбора данных. `/cart` (решено 2026-09-06, `docs/architecture.md`, раздел 2.3)
  — исключение из этого правила, но только в своих границах: client-side корзина
  (`localStorage`, без сервера/БД, без API-роута), и на самой странице `/cart` кнопка
  «Отправить на email» осознанно использует `mailto:` (сводка уходит из собственного
  почтового клиента клиента, не через сервер сайта) — вне `/cart` и `/contacts` запрет на
  `tel:`/`mailto:` остаётся в силе.
- Не менять роутинг, структуру папок или контракты API относительно того, что зафиксировано
  в `docs/`, без явного обновления соответствующего документа.
- Не реализовывать личный кабинет / авторизацию — отложено за пределы этого этапа.
- Изображения — только через `next/image`; домены — в `next.config.js` (`images.remotePatterns`).
- Цена товара нигде не берётся из `Product` — в типе её нет. Цену и КП менеджер готовит
  вручную вне сайта; автогенерации PDF КП в проекте больше нет (юридическое решение
  от 2026-08-31).
- Не хардкодить подписи статуса товара в компонентах — брать из
  `PRODUCT_AVAILABILITY_LABELS` (`types/product.ts`).
- Анимации — только Tailwind `transition`/CSS (fade/translate/blur/scale), без сторонних
  анимационных библиотек (Framer Motion и т.п.).
- Naming: роуты и файлы — `kebab-case`, компоненты — `PascalCase`, хуки — `useCamelCase`.
- SEO/метаданные — только через встроенный `generateMetadata`, отдельную библиотеку не добавлять.
- Комментарии обязательны на нетривиальных участках логики (см. `docs/coding-rules.md`).
  Прежние обязательные точки (валидация формы заявки, генерация PDF КП, загрузка в Vercel
  Blob) отпали вместе с отменой формы заявки.

## Карта документации (`docs/`)
Каждый файл — источник истины для своей области; при работе над соответствующей частью
проекта сначала читать его, а не полагаться на пересказ здесь.

| Файл | Отвечает за |
|---|---|
| `docs/architecture.md` | роутинг (App Router), источник данных каталога, разделение страниц |
| `docs/project-structure.md` | точная структура файлов/папок `src/`, куда класть что |
| `docs/tech-stack.md` | стек, деплой на Vercel, переменные окружения, Vercel Blob |
| `docs/coding-rules.md` | стиль кода, обработка ошибок, требования к комментариям |
| `docs/api.md` | **УСТАРЕЛ** (юридическое решение от 2026-08-31) — описывал отменённый флоу `POST /api/request` → PDF КП; хранится для истории |
| `docs/product.md` | структура `Product`/`ProductDraft`, правила заполнения карточки (`types/product.ts`) |
| `docs/frontend.md` | дизайн-система (цвет/типографика/сетка), структура компонентов `ui`/`layout`/`features`, требования к главной (слайдер, полоса логотипов) |
| `docs/seo.md` | SEO-пакет: `metadataBase`, `sitemap.ts`/`robots.ts` из каталога, `generateStaticParams`, `alt` категорий, `opengraph-image`, JSON-LD (`Product`, `BreadcrumbList`) |
| `docs/commercial-offer.md` | **УСТАРЕЛ** (юридическое решение от 2026-08-31) — описывал отменённую форму заявки и генерацию PDF КП; хранится для истории |

## Порядок работы
1. Перед написанием кода в любой области — прочитать соответствующий файл `docs/` из таблицы
   выше целиком, не фрагментом.
2. Если решения в `docs/` не хватает для конкретной технической детали — не додумывать и не
   выбирать самостоятельно между вариантами, а спросить пользователя.
3. Архитектурные решения принимаются только в `docs/`. Claude Code — исполнитель зафиксированного,
   не самостоятельный архитектор: не менять контракт API, схему `Product`, роутинг или дизайн-токены
   по собственной инициативе.
4. Если при реализации всплывает противоречие между файлами `docs/` (не должно — последний аудит
   подтвердил согласованность, но новые правки могли внести расхождение) — не выбирать сторону
   самостоятельно, сообщить пользователю.
5. Любое новое архитектурное решение, принятое по ходу разработки (в диалоге с пользователем),
   сначала фиксируется в соответствующем `docs/*.md`, и только потом попадает в код.

## Известные открытые вопросы (не решать самостоятельно)
- Отдельное поле `certification` в схеме товара — пока данные кладутся в `specs`/`documents`
  (см. `docs/product.md`)
- Финальный список полей `CategoryFilters` на фронте — зависит от `specs` конкретной категории
  (см. `docs/frontend.md`)
- Вопрос «источник цены для КП» снят: КП готовит менеджер вручную вне сайта (юридическое
  решение от 2026-08-31)

UX-улучшения из раздела D внешнего аудита (lightbox галереи, skeleton-загрузка, sticky CTA,
кнопка «наверх») реализованы 2026-09-05 — см. `docs/frontend.md`, раздел 4.6; открытым
вопросом больше не являются.

По итогам пяти независимых дизайн-ревью 2026-09-06: изменён цвет бейджа `available`
(зелёный → нейтральный/`accent-teal`) и текст CTA карточки товара («Связаться с
менеджером» → «Запросить КП», кнопка шапки не изменилась) — см. `docs/frontend.md`,
разделы 4.1 и 4.7, и `docs/product.md`, раздел 2.2.

Идея «корзины» (ранее значилась здесь открытым вопросом) решена в диалоге с
пользователем, 2026-09-06: `/cart`, client-side, `localStorage`, без сервера/БД — см.
`docs/architecture.md`, раздел 2.3, `docs/project-structure.md` и `docs/frontend.md`,
раздел 7. Открытым вопросом больше не является; правки выше (правила кода, флоу клиента)
уже отражают это решение.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
