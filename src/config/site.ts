// Единый источник констант сайта (Architecture.md, 2.2): название, описание для
// метаданных по умолчанию, базовый адрес, контактные данные. Только данные, без логики.
//
// `url` — плейсхолдер-домен Vercel (имя проекта `b2b-site` из package.json), от него
// строятся абсолютные адреса для metadataBase, sitemap, robots и JSON-LD (SEO.md,
// раздел 2). Перед деплоем сверить фактический Production-адрес в Vercel Dashboard;
// после подключения собственного домена — заменить здесь, в одном месте.
//
// Контакты сейчас — временные заглушки (реальные реквизиты компании ещё не
// переданы). Когда они появятся, значения меняются здесь, а не по отдельности
// в Header / Footer / странице «Контакты».

export const siteConfig = {
  name: "B2B-магазин промышленного оборудования",
  description:
    "Промышленное и специализированное оборудование под заказ из Китая: газоанализаторы, КИП, лабораторное оборудование, спецтехника.",
  url: "https://b2b-site.vercel.app",
  contacts: {
    phone: "+7 (000) 000-00-00 (временная заглушка)",
    email: "info@example.com (временная заглушка)",
  },
} as const;
